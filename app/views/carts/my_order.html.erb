<h1>my_order</h1>
<table class = "table" >
  <tr>
    <th>order_id</th>
    <th>title</th>
    <th>payment</th>
  </tr>
 
  <% @orders.each do |order| %>
    <% p=order.carts.first %>
    <% pro=Product.find(p.product_id) %>
    <tr>
      <td><%= order.id %></td>
      <td><%= pro.title %></td>    
      <td> done </td>
    </tr>
  <% end %>
</table>
